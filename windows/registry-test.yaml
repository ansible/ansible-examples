---
- name: test windows registry module
  hosts: all
  connection: winrm
  vars:
    ansible_ssh_port: 5986
  
  tasks:
    - name: change registry
      win_regedit:
        path: HKCU:\Software\Ansible
        state: present
