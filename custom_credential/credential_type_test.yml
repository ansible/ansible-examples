---
- name: Hello World Sample
  hosts: all
  tasks:
    - name: Print Username
      debug:
        msg: "{{ admin }}"
        
        
- name: Hello World Sample
  hosts: all
  tasks:
    - name: Print Password
      debug:
        msg: "{{ password }}"
