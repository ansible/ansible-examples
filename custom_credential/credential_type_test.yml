---
- name: Hello World Sample
  hosts: all
  tasks:
    - name: Print Username
      debug:
        msg: "{{ password }}"
        
        
- name: Hello World Sample
  hosts: all
  tasks:
    - name: Print Password
      debug:
        msg: "{{ password }}"
